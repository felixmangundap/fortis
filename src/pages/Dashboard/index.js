import React, { Component } from 'react';

class Dashboard extends Component {
  state = {
  };

  componentDidMount() {
  }

  render() {
    return (
      <div>
        Dashboard
      </div>
    );
  }
}

export default Dashboard;